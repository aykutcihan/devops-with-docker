# Exercise 2.2 - Getting Started

Read the documentation and figure out the command to enable the `simple-web-service` to respond to browser (or curl). Use the same service from the previous exercise.

> Hint: you will need to publish a port

Submit the `docker-compose.yaml`.
