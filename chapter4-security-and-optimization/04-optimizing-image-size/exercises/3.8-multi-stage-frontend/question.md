# Exercise 3.8 - Multi-Stage Frontend

Do a multi-stage build for the example frontend.

- Build the app following the README instructions (output goes to `build/` folder)
- The final image should not contain the source code — only the built assets
- Serve the static files with `serve` or another static file server

Submit the Dockerfile.
