# Exercise 3.9 - Multi-Stage Backend

Do a multi-stage build for the example backend (Golang).

- Build the binary in the first stage
- Use `FROM scratch` as the final image
- The resulting image must be **smaller than 35 MB**

Submit the Dockerfile.
