# Exercise 3.5 - Non-Root User

In this exercise you will use a non-root user in a Dockerfile.

Choose one of the example applications (e.g., `example-frontend` or `example-backend`) and add a non-root user to its Dockerfile. The user should be used to run the application.

Make sure the application still works after the change.

Submit the Dockerfile.
