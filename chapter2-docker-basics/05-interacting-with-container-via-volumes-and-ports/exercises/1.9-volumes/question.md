# Exercise 1.9 - Volumes

Image `devopsdockeruh/simple-web-service` creates a timestamp every two seconds to `/usr/src/app/text.log` when it's not given a command. Start the container with a bind mount to this file so that the logs are created into your filesystem.

As the answer submit the command(s) you used to complete the exercise.
